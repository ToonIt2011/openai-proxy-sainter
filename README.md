# OpenAI Proxy para Sainter

Este projeto serve como ponte entre a aplicação frontend (Lovable/Sainter) e a API da OpenAI.

### Endpoint:
POST /api/generate

Body JSON:
{
  "prompt": "Escreva um e-mail de agradecimento."
}
// trigger deploy
